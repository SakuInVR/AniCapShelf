[project]
name = "anicapshelf"
version = "0.1.0"
description = "アニメキャプチャと録画アーカイブを結びつけるローカルファーストなシーン索引"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]

[project.scripts]
anicapshelf = "anicapshelf.cli:main"

[tool.ruff]
line-length = 100
